version: "3.5"

services:

  frontend:
    build:
      context: ./Frontend
      dockerfile: ./Dockerfile
    ports:
      - 4205:4200

  backend:
    build:
      context: ./Backend/TestDemoApp
      dockerfile: ./Dockerfile
    ports:
      - 5005:80

